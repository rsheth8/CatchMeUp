# Secrets — never commit API keys
.env
.env.local
.env.*.local

# Python
venv/
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.Python

# Pipeline working dirs (keep the folders via .gitkeep)
recordings/*
!recordings/.gitkeep
output/*
!output/.gitkeep
processed/*
!processed/.gitkeep
logs/*
!logs/.gitkeep
# Specialist brains (personas + recaps stay on the user's machine)
brains/**
!brains/README.md

# OS / editor
.DS_Store
.AppleDouble
.LSOverride
.idea/
.vscode/
*.swp

# Local lecture corpus (multi-GB OCW rips — never commit)
MIT-6.0001/
MIT-6.0002/

# Local agent / IDE config
.claude/
.cursor/
.pytest_cache/
.coverage

# CLI distribution outputs (never user libraries)
build/
dist/

# Local course downloads (multi-MB video — never commit)
GraphQL-Crash-Course/
